---
name: sdlc-docs-jp
description: 雑多に書かれたメモ（アイデア出し）から、要求仕様書→要件定義書→基本設計書→詳細設計書という日本のソフトウェア開発工程の文書一式を、一貫したID体系（REQ-xxx/RF-xxx/DF-xxx/S-xxx/M-xxx）でトレーサビリティを保ちながら作成・レビューするためのスキル。ユーザーが「要求仕様書を作って」「要件定義書のテンプレートで」「基本設計書」「詳細設計書」「雑多メモから設計書に起こして」などと言った場合、あるいは日本語のSIer/受託開発スタイルの開発ドキュメント一式が必要な場面では、質問されなくてもこのスキルを積極的に使うこと。単発のテキスト整形依頼ではなく、要件〜設計の一連の文書を作る/レビューする文脈で必ず使う。
---

# 日本語SDLC文書作成スキル（雑多メモ→要求仕様書→要件定義書→基本設計書→詳細設計書）

## このスキルの目的

ユーザーが雑多に書いたメモ（アイデア出し）を出発点に、以下の5文書を順に作成する。各文書は前工程を受けて詳細化され、IDによって相互参照できる状態を保つ。

1. 雑多メモ（自由記述）
2. 要求仕様書（REQ-xxx）
3. 要件定義書（RF-xxx）
4. 基本設計書（DF-xxx / S-xxx＝画面ID）
5. 詳細設計書（M-xxx＝モジュールID）

テストやコードの生成を依頼された場合は、詳細設計書の内容（テーブル定義・処理ロジック・単体テスト観点）を根拠に、このスキルの範囲外の通常の実装作業として対応する（このスキル自体はドキュメント生成が主目的）。

## ID体系（最重要ルール）

各文書のIDは、後工程からの「関連◯◯」列で必ず前工程を参照する。プレフィックスは以下で固定する。ユーザーから別の命名指定がない限りこれを使う。

| 文書 | 対象 | プレフィックス | 例 |
|---|---|---|---|
| 要求仕様書 | 要求 | `REQ-xxx` | REQ-001 |
| 要件定義書 | 機能 | `RF-xxx`（Requirement Function） | RF-001 |
| 基本設計書 | 機能（設計単位） | `DF-xxx`（Design Function） | DF-001 |
| 基本設計書 | 画面 | `S-xxx` | S-001 |
| 詳細設計書 | モジュール | `M-xxx` | M-001 |

**トレーサビリティの流れ**：
`雑多メモの該当箇所` → `REQ-xxx`（要求仕様書） → `RF-xxx`（要件定義書の機能、関連要求REQ-xxxを持つ） → `DF-xxx`（基本設計書の機能、関連機能RF-xxxを持つ） → 画面(S-xxx)・帳票・テーブルはDF-xxxを参照 → `M-xxx`（詳細設計書のモジュール、関連する基本設計機能ID＝DF-xxxを持つ）

IDを持つ列・見出しには、必ず `(接頭辞-xxx)` の書式ヒントを付ける（例：`機能ID(RF-xxx)`）。これにより記入者が採番ルールを迷わない。

## 非機能項目の分類名（統一ルール）

要求仕様書・要件定義書・基本設計書の3文書で、非機能項目の分類名と並び順を必ず揃える。基準の並びは以下：

性能 → セキュリティ → 可用性 → 拡張性 → 運用・保守 → 移行

基本設計書は設計レベルの詳細項目（可用性・冗長化、バックアップ・リストア、監視・ログなど）を追加してよいが、上記6分類の言葉自体はぶれさせない。「運用・監視」のような表記ゆれを作らないこと。

## 使い方

### 1. ゼロから作成する場合

1. `assets/00_raw_notes_template.md` を提示し、ユーザーに雑多メモとして自由記述してもらう（既にメモがある場合はスキップ）
2. メモの内容を読み、`assets/01_requirements_spec_template.md` を土台に要求仕様書を作成する。各要求には「関連メモ」列にメモの該当箇所を書き、`REQ-001` から連番でIDを振る
3. 要求仕様書を土台に、`assets/02_requirements_definition_template.md` を使って要件定義書を作成する。機能ごとに `RF-xxx` を振り、「関連要求(REQ-xxx)」で対応する要求と紐付ける
4. 要件定義書を土台に、`assets/03_basic_design_template.md` を使って基本設計書を作成する。機能ごとに `DF-xxx` を振り、「関連機能(RF-xxx)」で要件定義書の機能と紐付ける。画面には `S-xxx` を振る
5. 基本設計書を土台に、`assets/04_detailed_design_template.md` を使って詳細設計書を作成する（機能・モジュールごとに複製）。モジュールには `M-xxx` を振り、「関連する基本設計機能ID(DF-xxx)」で紐付ける

### 2. 既存のメモ・文書から作成する場合

ユーザーが雑多なテキストファイルをアップロードした場合、その内容を読み込み、上記2〜5の手順で該当する文書から生成する（すでに要求仕様書がある場合は要件定義書からで良い、など、ユーザーの手持ち文書に応じて開始工程を判断する）。

### 3. レビューを依頼された場合

以下の観点でチェックする（このスキルを使う中で実際に有効だった観点）。

- **前工程とのトレーサビリティが途切れていないか**：機能・要求・データ・画面・帳票が相互にIDで参照できているか
- **ID書式ヒントの有無**：IDを持つ列見出しに `(接頭辞-xxx)` があるか
- **非機能項目の分類名・並び順の一貫性**：文書間で用語がぶれていないか（例：「運用・保守」と「運用・監視」の混在）
- **重複・冗長なセクション**：同じ内容を別の見出しで二重に書かせていないか
- **粒度の見落とし**：優先度、必須/任意、担当者など、後工程の判断材料になる列が欠けていないか
- **文書間のセクション構成の対称性**：用語定義・添付資料など、他の文書にあるのに欠けているセクションがないか

レビューでは、良い点→気になる点（優先度をつけて）→総評、の順で簡潔に伝え、対応するかどうかをユーザーに確認してから編集する。指摘した番号に対して「n番だけ対応して」と言われたら、その番号のみ対応する。

## 出力形式

指定がなければMarkdown（.md）で出力する。ファイル名は英数字（例：`01_requirements_spec.md`）にし、日本語ファイル名は使わない（ユーザーが英語ファイル名を希望する場合が多いため）。

## テンプレート本体

`assets/` フォルダの5ファイルをそのまま複製して使う。内容を直接読み込んで編集し、雛形の項目（背景、機能一覧、非機能要件など）をユーザーの入力に基づいて埋めていく。
